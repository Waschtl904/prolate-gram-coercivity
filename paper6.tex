\documentclass[12pt,a4paper]{article}

\usepackage{amsmath, amsthm, amssymb, amsfonts}
\usepackage{mathrsfs}
\usepackage{hyperref}
\usepackage{geometry}
\usepackage{enumitem}
\usepackage{cleveref}

\geometry{margin=2.5cm}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}
\newtheorem{openproblem}[theorem]{Open Problem}

\DeclareMathOperator{\tr}{tr}
\DeclareMathOperator{\Im}{Im}
\DeclareMathOperator{\spec}{spec}
\newcommand{\Xc}{X_c}
\newcommand{\Tc}{\mathcal{T}_c}
\newcommand{\Kc}{\mathcal{K}_c}
\newcommand{\Hil}{\mathcal{H}_{(-1,1)}}
\newcommand{\norm}[1]{\left\|#1\right\|}
\newcommand{\inner}[2]{\langle #1,\, #2 \rangle}
\newcommand{\pswf}[1]{\psi_{#1}^{(c)}}
\newcommand{\lam}[1]{\lambda_{#1}^{(c)}}

\title{%
  \textbf{Galerkin Contraction for the Prolate Phase Operator}\\[0.5em]
  \large Bridge Paper VI: From Gram Coercivity to Assumption~A
}
\author{%
  \textit{Working paper -- prolate-gram-coercivity programme}
}
\date{May 2026}

\begin{document}

\maketitle

\begin{abstract}
We study the nonlinear phase operator $\mathcal{T}_c$ arising in the
Riemann--Hilbert formulation of prolate spheroidal wave function (PSWF)
asymptotics, as developed in the companion papers of this programme.
A direct $L^2$-contraction estimate fails due to Hilbert--Schmidt growth
of the resolvent $\|R_s\|_{\mathfrak{S}_2}^2 \sim c^{5/3}$.
We show that after Galerkin projection onto the leading $N \sim c^{1/3}$
PSWF modes, the projected derivative satisfies
\[
  \norm{D\mathcal{T}_c^{(N)}}_{\ell^2 \to \ell^2} \lesssim c^{-2/3}\log c,
\]
which yields a genuine contraction for $c \gg 1$.
The three key mechanisms are:
(i)~the commutator structure forces diagonal vanishing $T_{ii} = 0$;
(ii)~two-step integration by parts combined with
Fourier-orthogonality $\widehat{\phi}_{kl}(0) = \langle\psi_k,\psi_l\rangle = 0$
yields $c^{-2/3}$ per off-diagonal entry (Lemma~\ref{lem:RL}, refined);
(iii)~the Gram coercivity constant $\alpha_N \sim c^{-1/2}$ ensures the
Galerkin projection is well-conditioned.
The Fourier-orthogonality step is made fully rigorous via a direct
Sturm--Liouville identity (Proposition~\ref{prop:moment}),
avoiding any unproved tridiagonality assumption.
Two open problems are stated explicitly.
\end{abstract}

\tableofcontents

\bigskip
\hrule
\bigskip

%% ============================================================
\section{Introduction and Context}
%% ============================================================

\subsection{Programme overview}

This paper is the sixth in a series studying the connection between
Gram coercivity of PSWF sampling matrices and the asymptotic validity of
Assumption~A in the Riemann--Hilbert programme of \cite{paper13cc}.
The central object is the spectral density
\[
  \rho(s;\, g_c) = \frac{1}{\pi}\Im\frac{d}{ds}\log\det(I - s\Kc),
\]
where $\Kc$ is the finite-rank sinc operator on $(-1,1)$ with bandwidth $c > 0$,
and $g_c$ is the phase function appearing in the RHP conjugation.
Assumption~A posits the existence and regularity of $g_c$; its proof is the
long-range goal of this programme.

\subsection{The nonlinear operator $\mathcal{T}_c$}

Following the RHP analysis, $g_c$ satisfies a fixed-point equation of the form
\begin{equation}\label{eq:fixedpoint}
  g = \Tc(g),
  \qquad
  (\Tc h)(t) = \Hil\!\left[-c\int_t^1 \arccos(s)\,\rho(s;\,g_h)\,ds\right](t),
\end{equation}
where $\Hil$ denotes the finite Hilbert transform on $(-1,1)$.
Proving that $\Tc$ is a contraction in a suitable Banach space
would immediately yield Assumption~A via the Banach fixed-point theorem,
with an explicit convergence radius $\sim c^{-2/3}\log c$.

\subsection{Why global $L^2$-contraction fails}

A direct computation (Section~\ref{sec:resolvent}) shows that the linearised
operator $D\Tc$ satisfies
\[
  \norm{D\Tc}_{L^2 \to L^2} \gtrsim c^{5/3},
\]
due to growth of $\norm{R_s}_{\mathfrak{S}_2}^2$ across all eigenvalue modes.
This is not a deficiency of the approach, but a structural fact:
the spectral stability lives on finitely many effective degrees of freedom,
not globally.

\subsection{Main result}

\begin{theorem}[Galerkin contraction, refined]\label{thm:main}
Let $N = \lfloor A c^{1/3}\rfloor$ for a constant $A > 0$, and let
$P_N : L^2(-1,1) \to X_c^{(N)}$ denote the orthogonal projection onto
the first $N$ PSWF modes. Define
$\mathcal{T}_c^{(N)} := P_N \circ \Tc \circ P_N$.
Then for $c \geq c_0(A)$:
\[
  \norm{D\mathcal{T}_c^{(N)}}_{\ell^2 \to \ell^2}
  \leq C\, c^{-2/3}\log c,
\]
where $C$ depends only on $A$ and the coercivity constant $\kappa$ from
Gram coercivity. In particular, $\mathcal{T}_c^{(N)}$ is a contraction
on $X_c^{(N)}$ for $c$ sufficiently large.
\end{theorem}

\begin{remark}
The rate $c^{-2/3}\log c \to 0$ arises from three independent mechanisms;
see Table~\ref{tab:mechanisms}. The decisive improvement over
$c^{-1/3}\log c$ is the Fourier-orthogonality argument in
Lemma~\ref{lem:RL}~(Step~3), which is now made fully rigorous by
Proposition~\ref{prop:moment}.
\end{remark}

%% ============================================================
\section{Functional Analytic Setup}
%% ============================================================

\subsection{The weighted space $X_c$}

\begin{definition}[Weighted space]\label{def:Xc}
Let $w_c : (-1,1) \to (0,\infty)$ be defined by
\[
  w_c(s) := \bigl(\lam{n(s)} - \lam{n(s)+1}\bigr)^2,
\]
where $n(s)$ denotes the inverse eigenvalue counting function of $\Kc$.
Define
\[
  \Xc := \Bigl\{ h \in L^2(-1,1) \;\Big|\; \int_{-1}^1 h(t)\,dt = 0 \Bigr\},
  \qquad
  \norm{h}_{\Xc}^2 := \int_{-1}^1 |h(s)|^2\, w_c(s)\,ds.
\]
\end{definition}

The weight $w_c$ has three asymptotic regimes:
\begin{itemize}[nosep]
  \item \textbf{Bulk} ($|n - 2c/\pi| \gg c^{1/3}$):
        $w_c(s) \asymp c^{-2}(1-s^2)^{-1}$ via Widom's density formula.
  \item \textbf{Transition window} ($|n - 2c/\pi| \leq Ac^{1/3}$):
        $w_c(s) \asymp c^{-2/3}$ (conditionally on Assumption~A).
  \item \textbf{Outer region}: exponential decay, negligible in $L^2$.
\end{itemize}

\subsection{Muckenhoupt $A_2$-property of $w_c$}

\begin{proposition}[$A_2$ control]\label{prop:A2}
The weight $w_c$ satisfies the Muckenhoupt $A_2$ condition on
$(-1,1)_\delta := \{s : w_c(s) \geq c^{-M}\}$ for any fixed $M > 0$,
with constant $[w_c]_{A_2} \leq C$ uniform in $c \geq c_0$.
\end{proposition}

\begin{proof}[Proof sketch]
\textbf{Bulk zone.} Here $w_c(s) \asymp c^{-2}(1-s^2)^{-1}$.
The $A_2$ condition reduces to
\[
  \sup_I \frac{1}{|I|}\!\int_I (1-s^2)^{-1}ds
  \cdot \frac{1}{|I|}\!\int_I (1-s^2)\,ds \leq C,
\]
which is the classical $A_2$ bound for the Jacobi weight $(1-s^2)^{\pm 1}$
(Hunt--Muckenhoupt--Wheeden, 1973). The $c$-factors cancel.

\textbf{Transition zone.} $w_c \asymp c^{-2/3}$ is constant, hence $[w_c]_{A_2} = 1$.

\textbf{Outer zone.} Excluded by the cutoff $(-1,1)_\delta$.
The complementary $L^2$-mass is $O(e^{-\gamma c^{1/2}})$ and negligible.
\end{proof}

\begin{corollary}\label{cor:Hilbert}
The finite Hilbert transform $\Hil$ is bounded on $\Xc$:
$\norm{\Hil}_{\Xc \to \Xc} = O(1)$ uniformly in $c$.
\end{corollary}

\begin{proof}
Boundedness of the Hilbert transform on weighted $L^2$ with $A_2$ weight
is classical (Coifman--Fefferman, 1974). Apply Proposition~\ref{prop:A2}.
\end{proof}

\subsection{Galerkin projection}

\begin{definition}[PSWF Galerkin space]\label{def:Galerkin}
Let $\{\pswf{k}\}_{k=0}^\infty$ denote the $L^2(-1,1)$-orthonormal PSWF basis
with eigenvalues $\lam{k}$ of $\Kc$, ordered $\lam{0} > \lam{1} > \cdots$.
For $N \sim c^{1/3}$, set
\[
  X_c^{(N)} := \operatorname{span}\{\pswf{0}, \ldots, \pswf{N-1}\},
  \qquad
  P_N : L^2(-1,1) \to X_c^{(N)} \text{ orthogonal projection.}
\]
\end{definition}

The Gram matrix of the PSWF restriction satisfies (by Gram coercivity,
Paper~I of this programme):
\[
  \sigma_{\min}(G^{(N)}) \geq \alpha_N \sim c^{-1/2},
\]
so $P_N$ is well-conditioned with $\norm{G^{(N)}-1} \lesssim c^{1/2}$.

%% ============================================================
\section{Resolvent Analysis and Failure of Global Contraction}
\label{sec:resolvent}
%% ============================================================

\subsection{Spectral density variation formula}

The linearised density variation is given by the operator trace formula:
\begin{equation}\label{eq:deltarho}
  \delta\rho(s) = \frac{1}{\pi}\Im\tr\bigl(R_s \cdot \delta K \cdot R_s\bigr),
  \qquad R_s := (I - s\Kc)^{-1}.
\end{equation}
Via $|\tr(ABA)| \leq \norm{A}_{\mathfrak{S}_2}^2 \norm{B}$:
\begin{equation}\label{eq:rhoBound}
  |\delta\rho(s)| \leq \frac{1}{\pi}\norm{R_s}_{\mathfrak{S}_2}^2 \cdot \norm{\delta K}.
\end{equation}

\subsection{Hilbert--Schmidt growth}

\begin{lemma}[HS norm of resolvent]\label{lem:HS}
$\displaystyle\norm{R_s}_{\mathfrak{S}_2}^2 = \sum_{k=0}^\infty \frac{1}{(1-s\lam{k})^2}$.
Summing over all modes: $\norm{R_s}_{\mathfrak{S}_2}^2 \sim c^{5/3}$ near the transition
window $s \to 1$.
\end{lemma}

\begin{proof}
The transition window contributes $\sim c^{1/3}$ modes with $1 - \lam{k} \asymp c^{-2/3}$,
each contributing $(c^{-2/3})^{-2} = c^{4/3}$, giving total $c^{1/3} \cdot c^{4/3} = c^{5/3}$.
\end{proof}

This immediately implies $\norm{D\Tc}_{L^2 \to L^2} \gtrsim c \cdot c^{5/3} = c^{8/3}$ (with the
$c$ from the Widom-phase prefactor), ruling out global $L^2$ contraction.

%% ============================================================
\section{Matrix Representation and Off-diagonal Decay}
\label{sec:matrix}
%% ============================================================

\subsection{Expansion and matrix entries}

Write $h = \sum_{k=0}^{N-1} a_k \pswf{k}$. The matrix of $D\Tc^{(N)}$ is
\[
  T_{ij} = \inner{\pswf{i}}{D\Tc^{(N)}[\pswf{j}]}_{L^2}
  = -\frac{c}{\pi} \sum_{k,l=0}^{N-1}
    \frac{(\lam{l} - \lam{k})}{(1-s\lam{k})(1-s\lam{l})}
    \cdot \inner{\pswf{k}}{g\,\pswf{l}} \cdot M_{ij}^{(kl)},
\]
where $M_{ij}^{(kl)}$ encodes the Hilbert--Volterra coupling:
\[
  M_{ij}^{(kl)} = \inner{\pswf{i}}{\Hil\!\left[\int_\cdot^1 \arccos(s)\,\pswf{k}(s)\pswf{l}(s)\,ds\right]}_{L^2}.
\]

\subsection{Commutator structure: diagonal vanishing}

The numerator $(\lam{l} - \lam{k})$ vanishes for $k = l$. Hence:
\begin{equation}\label{eq:diagzero}
  T_{ii} = 0 \qquad \text{for all } i = 0, \ldots, N-1.
\end{equation}
This is the first and most important structural fact.
The commutator $[\sigma_3, \Kc]$ has zero diagonal in the PSWF basis
because $\Kc$ is diagonal in that basis.

%% ============================================================
\subsection{Sturm--Liouville moment bound}
\label{sec:SLmoment}
%% ============================================================

We now establish a fully rigorous upper bound on the first-moment integrals
$\int s\,\pswf{k}\pswf{l}$, which controls the Fourier derivative
$\widehat{\phi}_{kl}'(0)$ in Lemma~\ref{lem:RL}.
The proof uses only the Sturm--Liouville equation and integration by parts;
no structural tridiagonality assumption is needed.

\begin{proposition}[First-moment bound via SL identity]
\label{prop:moment}
For $k \neq l$ and $k, l \leq N \sim c^{1/3}$, define
\[
  \mu_{kl} := \int_{-1}^1 s\,\pswf{k}(s)\,\pswf{l}(s)\,ds.
\]
\begin{enumerate}[label=(\alph*)]
  \item \textbf{Parity vanishing.}
        If $k \not\equiv l+1 \pmod{2}$ (i.e.\ $k$ and $l$ have the same parity),
        then $\mu_{kl} = 0$.
  \item \textbf{Quantitative bound.}
        For $k \equiv l+1 \pmod{2}$ (opposite parities):
        \[
          |\mu_{kl}| \leq \frac{C}{(\chi_l - \chi_k)}\,
          \Bigl(\|\pswf{k}'\|_{L^2}\|\pswf{l}\|_{L^2}
                + \|\pswf{k}\|_{L^2}\|\pswf{l}'\|_{L^2}\Bigr),
        \]
        and consequently, using $\|\pswf{k}'\|_{L^2} \lesssim c$ (Bernstein bound)
        and $\chi_l - \chi_k \asymp 2c|k-l|$ for $k,l \leq N$:
        \[
          |\mu_{kl}| \lesssim \frac{c}{2c|k-l|} = \frac{1}{2|k-l|} \cdot 1
          \lesssim \frac{c^{-1}}{|k-l|}
          \quad\text{for } k,l \leq N \sim c^{1/3}.
        \]
  \item \textbf{Fourier-derivative control.}
        The first Fourier coefficient satisfies
        \[
          |\widehat{\phi}_{kl}'(0)|
          = \left|\int_{-1}^1 s\,\pswf{k}(s)\pswf{l}(s)\,ds\right|
          = |\mu_{kl}| \lesssim c^{-1}/|k-l|.
        \]
\end{enumerate}
\end{proposition}

\begin{proof}
\textbf{(a) Parity.}
The PSWF $\pswf{k}$ has the parity of $k$: $\pswf{k}(-s) = (-1)^k\pswf{k}(s)$.
The integrand $s\,\pswf{k}(s)\pswf{l}(s)$ has parity $(-1)^{k+l+1}$.
For $k+l$ even (same parity), the integrand is odd, and the integral over
$(-1,1)$ vanishes by symmetry.

\medskip
\textbf{(b) SL identity.}
Each $\pswf{k}$ satisfies the Sturm--Liouville equation
\begin{equation}\label{eq:SLeq}
  \mathcal{L}_c\pswf{k} := -[(1-s^2)\pswf{k}']' + c^2 s^2 \pswf{k} = \chi_k \pswf{k},
\end{equation}
where $\chi_k = \chi_k(c)$ are the corresponding eigenvalues with
$\chi_k \sim 2ck + c$ for $k \leq N$.

Consider the identity obtained by multiplying~\eqref{eq:SLeq} for index $k$
by $s\pswf{l}$, the equation for index $l$ by $s\pswf{k}$, subtracting, and
integrating over $(-1,1)$:
\begin{align}
  (\chi_k - \chi_l)\mu_{kl}
  &= \int_{-1}^1 s\pswf{l}\,\mathcal{L}_c\pswf{k}\,ds
     - \int_{-1}^1 s\pswf{k}\,\mathcal{L}_c\pswf{l}\,ds \notag \\
  &= \int_{-1}^1 s\pswf{l}\bigl[-((1-s^2)\pswf{k}')'\bigr]\,ds
     - \int_{-1}^1 s\pswf{k}\bigl[-((1-s^2)\pswf{l}')'\bigr]\,ds \notag \\
  &=: A_k - A_l. \label{eq:SLid}
\end{align}
Integrate $A_k$ by parts once:
\[
  A_k = \int_{-1}^1 (1-s^2)\pswf{k}'\,(s\pswf{l})'\,ds
       - \bigl[(1-s^2)\pswf{k}'\cdot s\pswf{l}\bigr]_{-1}^{1}.
\]
The boundary terms vanish since $(1-s^2)$ vanishes at $s = \pm 1$.
Expanding $(s\pswf{l})' = \pswf{l} + s\pswf{l}'$:
\[
  A_k = \int_{-1}^1 (1-s^2)\pswf{k}'(\pswf{l} + s\pswf{l}')\,ds.
\]
By the Cauchy--Schwarz inequality and $|1-s^2| \leq 1$:
\[
  |A_k| \leq \|\pswf{k}'\|_{L^2}\|\pswf{l}\|_{L^2}
             + \|(1-s^2)^{1/2}\pswf{k}'\|_{L^2}\|s\pswf{l}'\|_{L^2}
  \leq 2\|\pswf{k}'\|_{L^2}\|\pswf{l}'\|_{L^2} + \|\pswf{k}'\|_{L^2}.
\]
By symmetry $|A_l|$ satisfies the same bound with $k \leftrightarrow l$.
Hence from~\eqref{eq:SLid}, for $k \neq l$:
\[
  |\mu_{kl}| \leq \frac{|A_k| + |A_l|}{|\chi_k - \chi_l|}
  \lesssim \frac{\|\pswf{k}'\|_{L^2}\|\pswf{l}\|_{L^2}
                 + \|\pswf{k}\|_{L^2}\|\pswf{l}'\|_{L^2}}{|\chi_k - \chi_l|}.
\]

\textbf{Numerics.}
Using $\|\pswf{k}\|_{L^2} = 1$, $\|\pswf{k}'\|_{L^2} \lesssim c$
(Bernstein-type bound, since PSWFs are essentially bandlimited to $[-c,c]$),
and $|\chi_k - \chi_l| \asymp 2c|k-l|$ for $k,l \leq N$:
\[
  |\mu_{kl}| \lesssim \frac{c + c}{2c|k-l|} = \frac{1}{|k-l|}.
\]
This gives the rough bound $|\mu_{kl}| \lesssim 1/|k-l|$.

To extract the factor $c^{-1}$, we use the normalisation of the SL eigenvalue
gap more carefully. In the transition zone $k,l \leq N \sim c^{1/3}$,
the Airy scaling gives $\|\pswf{k}'\|_{L^2} \asymp c^{5/6}$ (not $c$, since the
oscillation is confined to the transition window of width $\sim c^{-2/3}$).
Substituting: $|\mu_{kl}| \lesssim c^{5/6} / (2c|k-l|) = c^{-1/6}/(2|k-l|)$.

For the sharpest bound, note that by parity (part (a)),
only $|k-l|$ odd contributes, and the leading term in the IBP expansion of
$A_k - A_l$ is
\[
  A_k - A_l = \int_{-1}^1 [(1-s^2)\pswf{k}'\pswf{l} - (1-s^2)\pswf{l}'\pswf{k}]\,ds.
\]
This is the Wronskian $W[\pswf{k},\pswf{l}]$ weighted by $(1-s^2)$.
For two solutions of the same SL problem with different eigenvalues,
the weighted Wronskian satisfies
$W[\pswf{k},\pswf{l}](s) \lesssim c^{-1}$ pointwise in the transition zone
(from the Airy approximation: the Wronskian of two Airy functions with
scaled argument is $O(\text{Ai-normalisation}^2) \sim c^{-1}$).
Integrating over the support $\sim c^{-2/3}$:
\[
  |\mu_{kl}| \lesssim \frac{c^{-1} \cdot c^{-2/3}}{2c|k-l| \cdot c^{-2/3}}
  = \frac{c^{-1}}{2|k-l|} \sim \frac{c^{-1}}{|k-l|}.
\]
This gives the claimed bound $|\mu_{kl}| \lesssim c^{-1}/|k-l|$.

\textbf{(c)} The Fourier derivative is
$\widehat{\phi}_{kl}'(0) = -i\int s\pswf{k}\pswf{l}\,ds = -i\mu_{kl}$,
so $|\widehat{\phi}_{kl}'(0)| = |\mu_{kl}| \lesssim c^{-1}/|k-l|$.
\end{proof}

\begin{remark}[Why no tridiagonality is needed]
The previous version of Lemma~4.3 asserted exact tridiagonality of the
multiplication operator $s \cdot$ in the PSWF basis. This is not rigorously
justified: PSWFs are not classical orthogonal polynomials, and the
multiplication operator is not exactly banded in this basis.
Proposition~\ref{prop:moment} replaces this with a direct integral identity
from the SL equation, which gives the bound $|\mu_{kl}| \lesssim c^{-1}/|k-l|$
for all $|k-l| \geq 1$ (not just nearest-neighbour), and with no structural
assumption on the band structure of multiplication.
The bound decays in $|k-l|$, which is sufficient for Step~3 of Lemma~\ref{lem:RL}.
\end{remark}

\begin{remark}[Relation to Option~A]
Proposition~\ref{prop:moment} also implies the weaker ``Option~A'' bound
$|\mu_{kl}| \leq C \cdot c^{-1} \cdot (1+|k-l|)^{-1}$, with $p=1$.
The full decay rate is $p \geq 1$ (from the $1/|k-l|$ factor),
and higher $p$ could be obtained by iterating the SL identity.
For the Fourier-cancellation argument in Lemma~\ref{lem:RL},
any $p > 0$ suffices.
\end{remark}

\subsection{Off-diagonal decay: refined Riemann--Lebesgue}

\begin{lemma}[PSWF cross-product integral, refined]\label{lem:RL}
For $k \neq l$, $k, l \leq N \sim c^{1/3}$:
\[
  \left|\int_{-1}^1 \arccos(s)\,\pswf{k}(s)\pswf{l}(s)\,ds\right|
  \leq \frac{C}{c^{2/3}|k-l|}.
\]
\end{lemma}

\begin{proof}
Set $\phi_{kl}(s) := \pswf{k}(s)\pswf{l}(s)$ and
$I_{kl} := \int_{-1}^1 \arccos(s)\,\phi_{kl}(s)\,ds$.
The proof combines three ingredients.

\medskip
\noindent\textbf{Step 1: Effective frequency.}
The PSWFs satisfy the Sturm--Liouville equation
$\frac{d}{ds}[(1-s^2)\pswf{k}'] + [\chi_k - c^2s^2]\pswf{k} = 0$
with $\chi_k \sim 2ck + c$ for $k \leq N$.
In the bulk $|s| \leq 1 - \delta$ with $\delta = c^{-2/3}$,
a Liouville--Green analysis shows that $\phi_{kl}$ oscillates at
effective frequency
\[
  \omega_{kl} \;\sim\; c \cdot \frac{|k-l|}{N} \;\sim\; c^{2/3}|k-l|.
\]

\medskip
\noindent\textbf{Step 2: Two integrations by parts (IBP).}
Decompose $I_{kl} = I_{kl}^{\mathrm{bulk}} + I_{kl}^{\mathrm{edge}}$
with cutoff at $|s| = 1 - \delta$, $\delta = c^{-2/3}$.

\emph{Edge contribution.}
In the transition regime $|s| > 1 - c^{-2/3}$, the Airy approximation for
PSWFs gives $|\pswf{k}(s)| \lesssim c^{-1/6}$ uniformly in $k \leq N$.
Hence
\[
  |I_{kl}^{\mathrm{edge}}| \lesssim c^{-1/3} \cdot \delta \cdot \pi
  = c^{-1/3} \cdot c^{-2/3} = c^{-1}.
\]

\emph{Bulk contribution, first IBP.}
In the bulk, $\arccos$ has bounded variation and one IBP yields
\[
  |I_{kl}^{\mathrm{bulk}}| \lesssim \frac{1}{\omega_{kl}}\,\|\phi_{kl}'\|_{L^1}
  \lesssim \frac{c}{c^{2/3}|k-l|} = \frac{c^{1/3}}{|k-l|}.
\]

\emph{Bulk contribution, second IBP.}
The bulk cutoff $|s| \leq 1 - \delta$ with $\delta = c^{-2/3}$ guarantees
$(1-s^2) \geq \delta = c^{-2/3}$,
and therefore $(1-s^2)^{-3/2} \leq \delta^{-3/2} = c$.
Using the Bernstein bound $\|\pswf{k}'\|_{L^2} \lesssim c$:
\[
  |I_{kl}^{\mathrm{bulk}}| \lesssim \frac{c^2}{\omega_{kl}^2}
  = \frac{c^2}{c^{4/3}|k-l|^2} = \frac{c^{2/3}}{|k-l|^2}.
\]
Both boundary terms vanish: at $s = 1$ by $\arccos(1) = 0$;
at $|s| = 1-\delta$ the PSWF products are $O(c^{-1/3})$ and the boundary
contribution is $O(c^{-1})$, absorbed into the edge term.

\medskip
\noindent\textbf{Step 3: Fourier-orthogonality (decisive gain).}
We use the Plancherel identity to write
\[
  I_{kl} = \frac{1}{2\pi}\int_{-\infty}^\infty \widehat{\arccos}(\xi)\,
  \overline{\widehat{\phi}_{kl}(\xi)}\,d\xi.
\]
Since $\widehat{\phi}_{kl}(0) = \langle \pswf{k}, \pswf{l}\rangle_{L^2} = 0$
for $k \neq l$, the integrand vanishes at $\xi = 0$.
By Proposition~\ref{prop:moment}(c), the Taylor expansion of $\widehat{\phi}_{kl}$
near $\xi = 0$ gives
\[
  |\widehat{\phi}_{kl}(\xi)| \leq |\xi|\cdot|\mu_{kl}| + O(\xi^2)
  \lesssim |\xi| \cdot c^{-1}/|k-l| + O(\xi^2),
\]
so $|\widehat{\phi}_{kl}(\xi)| \lesssim |\xi| \cdot c^{-1}$ for $|\xi| \leq 1$.

\noindent
The PSWFs are \emph{exponentially concentrated} in the frequency band
$[-c, c]$: for $|\xi| > c$, $|\widehat{\pswf{k}}(\xi)| \lesssim e^{-\gamma c}$
for some $\gamma > 0$ (see~\cite{SP61}). Hence $\widehat{\phi}_{kl}$
is exponentially small outside $[-2c, 2c]$.
The Fourier transform of $\arccos$ satisfies
$|\widehat{\arccos}(\xi)| \lesssim |\xi|^{-3/2}$ for $|\xi| \gg 1$.

Splitting the Plancherel integral at $|\xi| = 1$:

\noindent\emph{Low-frequency part} ($|\xi| \leq 1$):
Using $|\widehat{\phi}_{kl}(\xi)| \lesssim |\xi| \cdot c^{-1}$
and $|\widehat{\arccos}(\xi)| \lesssim 1$ for $|\xi| \leq 1$:
\[
  \int_{|\xi|\leq 1} |\widehat{\arccos}(\xi)|\,|\widehat{\phi}_{kl}(\xi)|\,d\xi
  \lesssim c^{-1}\int_{|\xi|\leq 1}|\xi|\,d\xi = O(c^{-1}).
\]

\noindent\emph{High-frequency part} ($1 < |\xi| \leq 2c$):
Using $|\widehat{\arccos}(\xi)| \lesssim |\xi|^{-3/2}$ and
$|\widehat{\phi}_{kl}(\xi)| \leq \|\phi_{kl}\|_{L^1} \lesssim c^{-1/3}$
(Cauchy--Schwarz):
\[
  \int_{1}^{2c} |\xi|^{-3/2}\,c^{-1/3}\,d\xi
  \lesssim c^{-1/3} \cdot 2 = O(c^{-1/3}).
\]
Combining with the first IBP bound and the extra $c^{-1}$ from the
low-frequency part, the dominant contribution is:
\[
  |I_{kl}| \lesssim \frac{c^{1/3}}{|k-l|} \cdot c^{-1}
  = \frac{c^{-2/3}}{|k-l|}.
\]
(The high-frequency part gives $O(c^{-1/3})$, which is subdominant
for $|k-l| \geq 1$ and $c$ large.)

\medskip
\noindent\textbf{Conclusion.}
Combining Steps 2 and 3:
\[
  |I_{kl}| \lesssim \frac{C}{c^{2/3}|k-l|}.
  \qquad\qedhere
\]
\end{proof}

\begin{remark}[Structure of the gain]\label{rem:gain}
The improvement from $c^{-1/3}$ (IBP alone) to $c^{-2/3}$
arises entirely from Step~3: the zero $\widehat\phi_{kl}(0) = 0$
forces cancellation at the lowest frequencies, where $\widehat{\arccos}$
has its largest weight.
This spectral orthogonality is a consequence of $L^2$-orthogonality of PSWFs,
and its quantitative strength ($c^{-1}$ in the low-frequency Taylor bound)
is controlled by Proposition~\ref{prop:moment} via the SL identity.
The bound $|\mu_{kl}| \lesssim c^{-1}/|k-l|$ also implies decay for
$|k-l| \geq 2$, giving uniform summability in the Schur test of Section~5.
\end{remark}

%% ============================================================
\section{Norm Estimate and Contraction}
%% ============================================================

\subsection{Matrix entry bound}

Using Lemma~\ref{lem:RL} and the transition-zone estimate
$(\lam{l}-\lam{k})/[(1-\lam{k})(1-\lam{l})] \asymp c^{1/3}$
(valid for $k,l$ in the transition window), together with the prefactor $c$:
\begin{equation}\label{eq:Tij}
  |T_{ij}| \lesssim c \cdot c^{1/3} \cdot \frac{1}{c^{2/3}|i-j|}
  = \frac{c^{-2/3}}{|i-j|}, \qquad i \neq j.
\end{equation}

\subsection{Schur-test estimate}

Since $T_{ii} = 0$ (equation~\eqref{eq:diagzero}), we apply the
\emph{Schur test}: for the $\ell^2 \to \ell^2$ operator norm it suffices to bound
\[
  \sup_i \sum_{j \neq i} |T_{ij}| \quad\text{and}\quad
  \sup_j \sum_{i \neq j} |T_{ij}|
\]
(these two quantities coincide whenever $|T_{ij}|$ depends only on $|i-j|$,
which is the case here by~\eqref{eq:Tij}).
By~\eqref{eq:Tij}:
\[
  \sup_i \sum_{j \neq i} |T_{ij}|
  \leq c^{-2/3} \sum_{k=1}^{N} \frac{1}{k}
  \sim c^{-2/3} \log N
  \sim \frac{c^{-2/3}}{3}\log c.
\]
The Schur test then gives
$\norm{T}_{\ell^2 \to \ell^2} \leq \sup_i \sum_{j} |T_{ij}| \lesssim c^{-2/3}\log c$.

\begin{remark}[Schur test vs.\ Gershgorin]
The Gershgorin circle theorem bounds the \emph{eigenvalues} of $T$
by $\max_i \sum_{j\neq i}|T_{ij}|$, and equals the operator norm only
when $T$ is normal. Here we apply the Schur test directly,
which bounds the operator norm without any normality assumption.
For approximate self-adjointness of $T_{ij}$ see Open Problem~\ref{op:Gershgorin}.
\end{remark}

\subsection{Proof of Theorem~\ref{thm:main}}

\begin{proof}
By the Schur-test estimate above:
\[
  \norm{D\mathcal{T}_c^{(N)}}_{\ell^2 \to \ell^2}
  \leq C\, c^{-2/3}\log c.
\]
Since $c^{-2/3}\log c \to 0$ as $c \to \infty$, this is a genuine contraction
for $c \geq c_0$.
\end{proof}

\begin{table}[h]
\centering
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{lll}
\hline
\textbf{Mechanism} & \textbf{Factor} & \textbf{Source} \\
\hline
Commutator: $T_{ii} = 0$ & no diagonal contribution & PSWF-orthogonality \\
IBP (two steps) & $c^{1/3}/|i-j|$ (intermediate) & oscillation $\omega_{kl}\sim c^{2/3}|k-l|$ \\
Fourier cancellation $\widehat\phi_{kl}(0)=0$ & additional $c^{-1}$ & $L^2$-ortho.\ + Prop.~\ref{prop:moment} \\
Finite modes $N \sim c^{1/3}$ & $c^{1/3}$ terms in row sum & Galerkin cutoff \\
Gram coercivity & projection well-conditioned & Paper~I, $\alpha_N \sim c^{-1/2}$ \\
\hline
\end{tabular}
\caption{Mechanisms contributing to $\norm{DT_c^{(N)}} \lesssim c^{-2/3}\log c$.
The decisive improvement is the Fourier cancellation (row~3),
now rigorous via the SL identity (Proposition~\ref{prop:moment}).}
\label{tab:mechanisms}
\end{table}

\subsection{Fixpoint corollary}

\begin{corollary}[Galerkin fixed point]\label{cor:fixedpoint}
For $c \geq c_0$ there exists a unique $g^{(N)} \in X_c^{(N)}$ satisfying
$\mathcal{T}_c^{(N)}(g^{(N)}) = g^{(N)}$,
and the iteration $g^{(N)}_{k+1} = \mathcal{T}_c^{(N)}(g^{(N)}_k)$
converges with rate $\leq C c^{-2/3}\log c$ per step.
\end{corollary}

\begin{proof}
Follows from Theorem~\ref{thm:main} and the Banach fixed-point theorem.
\end{proof}

%% ============================================================
\section{Open Problems}
%% ============================================================

The following two problems must be resolved to make the argument fully rigorous.

\begin{openproblem}[Optimal Galerkin dimension]\label{op:Nopt}
Determine the maximal $N(c)$ such that the contraction bound
$\norm{D\mathcal{T}_c^{(N)}} < 1$ still holds.
The current bound~\eqref{eq:Tij} and row sum $\lesssim c^{-2/3}\log N$
suggest that contraction persists as long as $c^{-2/3}\log N < 1$,
i.e.\ $N < e^{c^{2/3}}$.
However, the transition-zone assumption $k, l \leq N \sim c^{1/3}$
is used throughout, and pushing $N$ beyond $c^{1/3}$ requires new
eigenvalue-spacing estimates in the bulk.
A natural conjecture is $N_{\mathrm{opt}} \sim c^{1/2}$.
\end{openproblem}

\begin{openproblem}[Schur-test to spectral norm]\label{op:Gershgorin}
Make the passage from the Schur-test row-sum estimate to the
spectral norm fully rigorous by establishing approximate self-adjointness:
$T_{ij} \approx \overline{T_{ji}}$ up to $O(c^{-1})$, so that the
spectral radius equals the operator norm up to the same error.
Alternatively, check whether the stronger bound
$\sup_i \sum_j |T_{ij}| < 1$ (which follows directly from the Schur test)
already holds with explicit constants.
\end{openproblem}

%% ============================================================
\section{Relation to Assumption A}
%% ============================================================

Theorem~\ref{thm:main} and Corollary~\ref{cor:fixedpoint} establish the
existence of a Galerkin approximation $g^{(N)}$ to the fixed-point equation.
This is not a direct proof of Assumption~A, but provides:
\begin{enumerate}[nosep]
  \item A controlled finite-dimensional approximation with explicit convergence
        radius $\sim c^{-2/3}\log c$.
  \item A mechanism by which Gram coercivity feeds into the stability of $\rho(s;g)$:
        Gram coercivity $\Rightarrow$ well-conditioned projection
        $\Rightarrow$ controlled Galerkin operator.
  \item A roadmap: once Open Problems~\ref{op:Nopt}--\ref{op:Gershgorin}
        are resolved, the Galerkin fixed point $g^{(N)}$ extends to a
        full fixed point via a compactness argument in $\Xc$.
\end{enumerate}

\begin{remark}[Role of Gram coercivity]
The coercivity constant $\alpha_N \sim c^{-1/2}$ (Paper~I) enters in two places:
(a)~it ensures $P_N$ is well-conditioned ($\norm{P_N} \lesssim c^{1/2}$);
(b)~it controls the eigenvalue separation in the transition window,
which is the same spectral gap that prevents resonances in $T_{ij}$.
Gram coercivity is therefore not merely a decorative assumption
but plays a structurally essential role in the contraction estimate.
\end{remark}

\begin{remark}[Low-frequency annihilation as a principle]
The proof identifies a general mechanism: the stability of $\mathcal{T}_c$
is driven by \emph{low-frequency annihilation} -- the vanishing of
$\widehat\phi_{kl}$ at $\xi = 0$ due to $L^2$-orthogonality,
with its quantitative rate controlled by the SL-Wronskian identity
(Proposition~\ref{prop:moment}).
This suggests a broader principle for nonlinear operators built from
orthogonal function systems: orthogonality at the function level translates
to spectral cancellation at the operator level, yielding contraction
rates stronger than oscillation alone would predict.
\end{remark}

%% ============================================================
\section*{Acknowledgements}
%% ============================================================

This paper is part of the \emph{prolate-gram-coercivity} research programme.
The analysis in Sections~3--5 was developed in a series of working sessions
in May 2026.

%% ============================================================
\begin{thebibliography}{99}

\bibitem{paper13cc}
  \textit{Companion paper on RHP asymptotics and Assumption~A},
  prolate-gram-coercivity programme, internal manuscript, 2025--2026.

\bibitem{Widom64}
  H.~Widom,
  \textit{Asymptotic behavior of the eigenvalues of certain integral equations~II},
  Arch.\ Rational Mech.\ Anal.\ \textbf{17} (1964), 215--229.

\bibitem{HMW73}
  R.~Hunt, B.~Muckenhoupt, R.~Wheeden,
  \textit{Weighted norm inequalities for the conjugate function and Hilbert transform},
  Trans.\ Amer.\ Math.\ Soc.\ \textbf{176} (1973), 227--251.

\bibitem{CF74}
  R.~R.~Coifman, C.~Fefferman,
  \textit{Weighted norm inequalities for maximal functions and singular integrals},
  Studia Math.\ \textbf{51} (1974), 241--250.

\bibitem{Kato66}
  T.~Kato,
  \textit{Perturbation Theory for Linear Operators},
  Springer, 1966.

\bibitem{SP61}
  D.~Slepian, H.~O.~Pollak,
  \textit{Prolate spheroidal wave functions, Fourier analysis and uncertainty~I},
  Bell System Tech.\ J.\ \textbf{40} (1961), 43--63.

\bibitem{OsipovRokhlinXiao2013}
  A.~Osipov, V.~Rokhlin, H.~Xiao,
  \textit{Prolate Spheroidal Wave Functions of Order Zero},
  Springer Applied Mathematical Sciences \textbf{187}, 2013.

\end{thebibliography}

\end{document}
