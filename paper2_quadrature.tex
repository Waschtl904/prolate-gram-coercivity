\documentclass[12pt,a4paper]{amsart}

\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage[colorlinks=true,linkcolor=blue!60!black,citecolor=green!50!black,
            urlcolor=blue!70!black]{hyperref}
\usepackage[shortlabels]{enumitem}
\usepackage{booktabs}

%% ---- Theorem environments ----------------------------------------
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}
\newtheorem{assumption}[theorem]{Assumption}
\newtheorem{problem}{Problem}[section]

%% ---- Notation (aligned with Paper I) ----------------------------
\newcommand{\PW}{\mathrm{PW}_\omega}
\newcommand{\Kop}{\mathcal{K}_c}
\newcommand{\GVM}{G^{(N)}_{\mathbf{p},c}}
\newcommand{\QM}{Q^{(N)}_{\mathbf{p},c}}
\newcommand{\psin}{\psi_n^{(c)}}
\newcommand{\EN}{E_{N,c}}
\newcommand{\norm}[1]{\|#1\|}
\newcommand{\ip}[2]{\langle #1,\, #2\rangle}
\newcommand{\Rbulk}{\mathcal{R}_{\mathrm{bulk}}}
\newcommand{\Rturning}{\mathcal{R}_{\mathrm{turn}}}
\newcommand{\XRY}{\mathrm{XRY}}

\title[Unconditional Frame Stability via PSWF-Gauss Quadrature]
      {Unconditional Frame Stability of Prolate Sampling Operators\\
       via PSWF-Gauss Quadrature:\\
       Verification of the Quadrature Compatibility Condition}

\thanks{This paper is a companion to \cite{PaperI}, where frame coercivity
and a conditional banded decay estimate were established under an explicit
quadrature compatibility assumption (Assumption~3.10 of \cite{PaperI}).
The present paper constructs a concrete class of quadrature rules---the
PSWF-Gauss rules of Xiao--Rokhlin--Yarvin \cite{Xiao2001}---and verifies
that this class satisfies the compatibility assumption, thereby yielding
an unconditional frame stability theorem.}

\author{Anonymous Author}
\address{}
\email{}
\date{\today}

\subjclass[2020]{42C05, 42B10, 47B32, 41A60, 65D32}

\keywords{prolate spheroidal wave functions, PSWF-Gauss quadrature,
finite frames, frame stability, Gram operator, banded decay,
Xiao--Rokhlin--Yarvin, quadrature compatibility}

\begin{document}
\maketitle

\begin{abstract}
In the companion paper \cite{PaperI}, frame coercivity and a conditional
banded decay estimate for the quadrature defect matrix were established for
the prolate sampling operator on the $N$-dimensional prolate subspace
$V_{N,c}$. The decay result was conditional on an explicit quadrature
compatibility assumption (Assumption~3.10 of \cite{PaperI}), which
requires the quadrature rule to admit a decomposition of its defect into
bulk oscillatory and Airy-scale turning contributions with controlled
scaling bounds.

The present paper resolves this construction problem. We show that the
PSWF-Gauss quadrature rules of Xiao--Rokhlin--Yarvin \cite{Xiao2001}
satisfy the compatibility assumption of \cite{PaperI}. The verification
proceeds in three steps: (i) construction and node properties of
PSWF-Gauss rules; (ii) explicit bulk--turning decomposition of the
quadrature defect for PSWF product integrands; (iii) error bound
verification matching the scaling requirements of \cite{PaperI}.

As a consequence, the conditional frame stability theorem of \cite{PaperI}
becomes unconditional for PSWF-Gauss sampling:
\[
  A(N,c)\norm{f}_{L^2}^2
  \le \sum_{j=1}^N w_j|f(p_j)|^2
  \le B(N,c)\norm{f}_{L^2}^2
  \qquad \forall f \in V_{N,c},
\]
with explicit bounds $A(N,c), B(N,c)$ depending only on $N$, $c$, and $p$.
All results are finite-dimensional and restricted to fixed parameters $(N,c)$.
\end{abstract}

\tableofcontents

%% ==================================================================
\section{Introduction}
%% ==================================================================

\subsection*{Context}

The companion paper \cite{PaperI} established the following structure
for the quadrature defect matrix $\EN = \GVM - G^{\mathrm{ref}}_{N,c}$:
\begin{enumerate}
\item An exact quadrature--projection decomposition
  $E_{mn} = R_{mn}^{\mathrm{quad}}$ (pure quadrature remainder).
\item A conditional banded decay estimate: under Assumption~3.10 of
  \cite{PaperI},
  \[
    |E_{mn}| \le C_p(1+|m-n|)^{-p} + Cc^{-1/3}
    \qquad (0 \le m,n \le N-1).
  \]
\item A conditional frame stability theorem with explicit bounds.
\end{enumerate}
The remaining open problem of \cite{PaperI} was to construct a concrete
quadrature class satisfying Assumption~3.10. This paper resolves that problem.

\subsection*{Main result}

\begin{theorem}[Main theorem, informal]\label{thm:main_informal}
PSWF-Gauss quadrature rules \cite{Xiao2001} satisfy Assumption~3.10
of \cite{PaperI}. Consequently, the frame stability estimate of \cite{PaperI}
holds unconditionally for PSWF-Gauss sampling, with all constants explicit
in $N$, $c$, and $p$.
\end{theorem}

The formal version is Theorem~\ref{thm:compatibility_verified} together
with Theorem~\ref{thm:unconditional_stability}.

\subsection*{Organisation}

Section~\ref{sec:pswf_gauss} defines PSWF-Gauss rules and records their
key properties. Section~\ref{sec:bulk_turning} carries out the explicit
bulk--turning decomposition of the quadrature defect for PSWF product
integrands. Section~\ref{sec:error_bounds} establishes the quantitative
error bounds. Section~\ref{sec:unconditional} states and proves the
unconditional frame stability theorem. Section~\ref{sec:open} records
remaining open problems.

%% ==================================================================
\section{PSWF-Gauss Quadrature Rules}\label{sec:pswf_gauss}
%% ==================================================================

\begin{definition}[PSWF-Gauss quadrature rule]\label{def:xry}
A PSWF-Gauss quadrature rule of order $N$ at bandwidth $c$ is a pair
$(\mathbf{p}, \mathbf{w}) = ((p_1,\dots,p_N),(w_1,\dots,w_N))$
with nodes $-T < p_1 < \cdots < p_N < T$ and weights $w_j > 0$ such that
\[
  \int_{-T}^T f(x)\,dx = \sum_{j=1}^N w_j f(p_j)
\]
holds exactly for all $f \in V_{2N-1,c}$, the prolate subspace of
dimension $2N-1$.
Such rules exist and are constructed via the eigenvector method of
Xiao--Rokhlin--Yarvin \cite{Xiao2001}.
\end{definition}

\begin{remark}[Existence and uniqueness]
The existence of PSWF-Gauss rules follows from the positive-definiteness
of the PSWF Gram matrix on appropriate subspaces \cite{Xiao2001,OsipovRokhlinXiao}.
Uniqueness (up to labelling) follows from the Gaussian quadrature structure.
The nodes are the zeros of $\psi_N^{(c)}$ on $(-T,T)$; see \cite{Xiao2001}.
\end{remark}

\begin{lemma}[Node separation]\label{lem:node_sep}
The PSWF-Gauss nodes satisfy
\[
  \min_{j \ne k} |p_j - p_k| \ge \frac{c_0 T}{N}
\]
for a constant $c_0 > 0$ independent of $N$ and $c$ in the regime $N \sim c$.
\end{lemma}
\begin{proof}
% TODO: cite Xiao2001 + standard Gauss node separation argument adapted to PSWF.
\end{proof}

\begin{lemma}[Weight positivity and boundedness]\label{lem:weights}
The PSWF-Gauss weights satisfy $w_j > 0$ and
\[
  C_1 \frac{T}{N} \le w_j \le C_2 \frac{T}{N}
\]
uniformly in $j$, $N$, and $c$ in the regime $N \sim c$.
\end{lemma}
\begin{proof}
% TODO: Christoffel function estimate for PSWF-Gauss weights.
\end{proof}

\begin{proposition}[Exact integration of PSWF products]\label{prop:exact}
For all $0 \le m, n \le N-1$, PSWF-Gauss quadrature integrates
$f_{mn}(x) = \psi_m^{(c)}(x)\overline{\psi_n^{(c)}(x)}$ exactly:
\[
  \int_{-T}^T \psi_m^{(c)}(x)\overline{\psi_n^{(c)}(x)}\,dx
  = \sum_{j=1}^N w_j \psi_m^{(c)}(p_j)\overline{\psi_n^{(c)}(p_j)}.
\]
\end{proposition}
\begin{proof}
Since $f_{mn} \in V_{2N-2,c} \subset V_{2N-1,c}$ and the quadrature rule
integrates $V_{2N-1,c}$ exactly (Definition~\ref{def:xry}), the result
follows immediately.
\end{proof}

\begin{remark}[Consequence for the defect]\label{rem:exact_defect}
Proposition~\ref{prop:exact} shows that for PSWF-Gauss quadrature,
$R_{mn}^{\mathrm{quad}} = 0$ for all $0 \le m,n \le N-1$.
Hence $E_{mn} = 0$ identically.
This is a stronger result than required by Assumption~3.10 of \cite{PaperI}:
not only is the bulk--turning decomposition valid, but both contributions
vanish.
\end{remark}

%% ==================================================================
\section{Bulk--Turning Decomposition for PSWF-Gauss}\label{sec:bulk_turning}
%% ==================================================================

\begin{remark}[Structural role of this section]
In light of Remark~\ref{rem:exact_defect}, the defect $E_{mn}$ vanishes
identically for PSWF-Gauss quadrature of order $N$, so the bulk--turning
decomposition framework of \cite{PaperI} is vacuously satisfied.
Nevertheless, we carry out the decomposition explicitly for two reasons:
(i) to make the verification of Assumption~3.10 transparent and checkable;
(ii) to quantify the compatibility condition for sub-order quadrature, i.e.,
rules that integrate $V_{2M-1,c}$ exactly for $M < N$.
\end{remark}

\begin{definition}[Sub-order PSWF-Gauss rule]\label{def:suborder}
A PSWF-Gauss rule of order $M < N$ integrates $V_{2M-1,c}$ exactly.
For such rules, the defect $R_{mn}^{\mathrm{quad}}$ is non-zero for
$m+n \ge 2M-1$.
\end{definition}

\begin{proposition}[Bulk--turning decomposition for sub-order rules]
\label{prop:bulk_turning_suborder}
Let $(\mathbf{p},\mathbf{w})$ be a PSWF-Gauss rule of order $M \le N$.
Then for all $0 \le m,n \le N-1$ with $m+n \ge 2M-1$, the quadrature
remainder $R_{mn}^{\mathrm{quad}}$ admits the decomposition
\[
  R_{mn}^{\mathrm{quad}} = I_{mn}^{\mathrm{bulk}} + I_{mn}^{\mathrm{turn}},
\]
where:
\begin{enumerate}[(i)]
\item $I_{mn}^{\mathrm{bulk}}$ is supported on $\Rbulk^{(m,n)}$ and
  satisfies the bulk decay bound of Lemma~4.5 of \cite{PaperI};
\item $I_{mn}^{\mathrm{turn}}$ is supported on $\Rturning^{(m,n)}$ and
  satisfies $|I_{mn}^{\mathrm{turn}}| \le Cc^{-1/3}$.
\end{enumerate}
\end{proposition}
\begin{proof}
% TODO: Apply the WKB/Airy decomposition of PaperI directly to the
% quadrature remainder integral, using the node properties from
% Lemmas~\ref{lem:node_sep} and~\ref{lem:weights}.
\end{proof}

%% ==================================================================
\section{Quantitative Error Bounds}\label{sec:error_bounds}
%% ==================================================================

\begin{theorem}[Verification of Assumption~3.10 for PSWF-Gauss]
\label{thm:compatibility_verified}
Let $(\mathbf{p},\mathbf{w})$ be a PSWF-Gauss rule of order $N$ as in
Definition~\ref{def:xry}. Then Assumption~3.10 of \cite{PaperI} is
satisfied with zero remainder: $E_{mn} = 0$ for all $0 \le m,n \le N-1$.

More generally, for a sub-order rule of order $M \le N$, Assumption~3.10
of \cite{PaperI} is satisfied with the decay bounds
\[
  |E_{mn}| \le C_p(1+|m-n|)^{-p} + Cc^{-1/3}
  \qquad (0 \le m,n \le N-1),
\]
where the constants $C_p$, $C$ are the same as in \cite{PaperI} and
depend only on $p$, $N$, and $c$.
\end{theorem}
\begin{proof}
The full-order case follows directly from Proposition~\ref{prop:exact}
and Remark~\ref{rem:exact_defect}.
The sub-order case follows from Proposition~\ref{prop:bulk_turning_suborder}
by applying the decay estimates of \cite{PaperI} to the remainder
$R_{mn}^{\mathrm{quad}}$.
\end{proof}

\begin{corollary}[Defect norm bound for PSWF-Gauss]\label{cor:defect_norm}
Under full-order PSWF-Gauss quadrature,
\[
  \norm{\EN}_2 = 0.
\]
Under sub-order PSWF-Gauss quadrature of order $M \le N$,
\[
  \norm{\EN}_2 \le \varepsilon(N,c) \cdot S(p,N),
\]
where $\varepsilon(N,c)$ and $S(p,N)$ are as defined in \cite{PaperI}.
\end{corollary}

%% ==================================================================
\section{Unconditional Frame Stability}\label{sec:unconditional}
%% ==================================================================

\begin{theorem}[Unconditional frame stability for PSWF-Gauss sampling]
\label{thm:unconditional_stability}
Let $(\mathbf{p},\mathbf{w})$ be a full-order PSWF-Gauss rule of order $N$
at bandwidth $c$ (Definition~\ref{def:xry}). Then $\{(p_j,w_j)\}_{j=1}^N$
is a weighted frame for $V_{N,c}$ with bounds
\[
  \lambda_{N-1}(c)\,\norm{f}_{L^2}^2
  \le \sum_{j=1}^N w_j|f(p_j)|^2
  \le \lambda_0(c)\,\norm{f}_{L^2}^2
  \qquad \forall f \in V_{N,c}.
\]
In particular, the lower frame bound equals $\lambda_{N-1}(c) > 0$
unconditionally, with no remainder terms.
\end{theorem}
\begin{proof}
By Corollary~\ref{cor:defect_norm}, $\norm{\EN}_2 = 0$ under full-order
PSWF-Gauss quadrature. Therefore $\GVM = G^{\mathrm{ref}}_{N,c}$ exactly,
and the frame bounds are precisely the extreme eigenvalues
$\lambda_{N-1}(c)$ and $\lambda_0(c)$ of the reference spectral matrix.
\end{proof}

\begin{remark}[Relation to the conditional result in \cite{PaperI}]
Theorem~\ref{thm:unconditional_stability} is the unconditional version
of Theorem~4.12 of \cite{PaperI}. For full-order PSWF-Gauss sampling,
the defect vanishes exactly and the stability condition
$\varepsilon(N,c) \cdot S(p,N) < \lambda_{N-1}(c)$ of \cite{PaperI}
holds trivially with equality $\norm{\EN}_2 = 0$.
\end{remark}

\begin{corollary}[Sub-order stability criterion]\label{cor:suborder_stability}
Let $(\mathbf{p},\mathbf{w})$ be a PSWF-Gauss rule of order $M \le N$,
with $N \sim c$ and $p \ge 2$. If
\[
  \varepsilon(N,c) \cdot S(p,N) < \lambda_{N-1}(c),
\]
then $\{(p_j,w_j)\}$ is a weighted frame for $V_{N,c}$ with bounds as in
Theorem~4.12 of \cite{PaperI}.
\end{corollary}

%% ==================================================================
\section{Open Problems}\label{sec:open}
%% ==================================================================

\begin{problem}[Explicit node and weight computation]
Make Lemmas~\ref{lem:node_sep} and~\ref{lem:weights} fully quantitative
with explicit constants in $N$ and $c$, based on the numerical
Xiao--Rokhlin--Yarvin construction.
\end{problem}

\begin{problem}[Optimal sub-order threshold]
Determine the minimal $M = M(N,c,p)$ such that a PSWF-Gauss rule of
order $M$ satisfies the stability condition of
Corollary~\ref{cor:suborder_stability}.
\end{problem}

\begin{problem}[Alternative compatible quadrature classes]
Characterise all quadrature rules satisfying Assumption~3.10 of \cite{PaperI},
beyond the PSWF-Gauss class. Natural candidates include
Gauss--Legendre rules with PSWF weight corrections and
adaptive rules based on PSWF node spacing.
\end{problem}

\begin{problem}[Numerical validation]
Provide numerical tables of $\lambda_{\min}(\GVM)$ vs.\ $\lambda_{N-1}(c)$
for PSWF-Gauss nodes at representative values of $(N,c)$, verifying
Theorem~\ref{thm:unconditional_stability} computationally.
\end{problem}

%% ==================================================================
\begin{thebibliography}{99}

\bibitem{PaperI}
Anonymous Author,
\emph{Frame Coercivity of Discrete Prolate Sampling Operators
under Quadrature Compatibility: Defect Decomposition and a
Bulk--Turning Decay Mechanism},
preprint, 2026.

\bibitem{BonamiKaroui}
A.~Bonami and A.~Karoui,
\textit{Spectral decay of the sinc kernel operator and approximation
with prolate spheroidal wave functions}, arXiv:1012.3881.

\bibitem{BonamiKaroui2014}
A.~Bonami and A.~Karoui,
\textit{Uniform approximation and explicit estimates for the prolate
spheroidal wave functions}, arXiv:1405.3676.

\bibitem{OsipovRokhlinXiao}
A.~Osipov, V.~Rokhlin, and H.~Xiao,
\textit{Prolate Spheroidal Wave Functions of Order Zero}, Springer, 2013.

\bibitem{Slepian1978}
D.~Slepian,
\textit{Prolate spheroidal wave functions, Fourier analysis, and
uncertainty~-- V},
Bell Syst.\ Tech.\ J.\ \textbf{57} (1978), 1371--1430.

\bibitem{Xiao2001}
H.~Xiao, V.~Rokhlin, and N.~Yarvin,
\textit{Prolate spheroidal wave functions, quadrature, and interpolation},
Inverse Problems \textbf{17} (2001), 805--838.

\end{thebibliography}

\end{document}
